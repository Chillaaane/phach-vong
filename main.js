const btn=document.querySelector('.menu'),nav=document.querySelector('.site-header nav');if(btn)btn.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
